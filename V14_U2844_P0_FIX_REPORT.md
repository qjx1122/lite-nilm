# U2844 P0 修复验证报告（OFF 日抑制 + 功率标定）

> **用户**: `800080252844_4206894986488` (变频 p2)  
> **版本**: v14.1.1 `bus_guard` (d87 日级启动签名 + 启动日双阈值 + 功率标定)  
> **验证窗**: 2026-07-01 ~ 2026-07-15 OOD  
> **命令**: `run_batch_users.py --users 800080252844... --time-filter-config data/time_filters.json`

---

## 1. 问题回顾（修复前）

| 日期 | 现象 | FP/FN | F1 |
|------|------|-------|-----|
| 7/02–7/04 | 全 OFF 日大段假开 (~730W) | 15+4+25 / 0 | 0.00 |
| 7/05 | 真 ON 几乎全漏 | 0 / 36 | 0.10 |
| 整体 | ood_F1=**0.897**, MAE=72.9W, SAE=11.9% | | |

根因：
1. d87 步级守卫对该变频用户关闭，缺少 OFF 日硬抑制。  
2. **总线 d73 抬升无法区分假开**（假开日 lift 与真 ON 同量级，来自其它大功率负荷）。  
3. 7/05 真 ON 段 `p_on` 中位 ~0.50 < `best_thr=0.79` → 阈值过严漏检。  
4. 模型 ON 功率系统性高估（pred/true≈1.8）。

---

## 2. 方案（v14.1.1）

| 模块 | 机制 | 参数 (U2844) |
|------|------|----------------|
| **A. 日级 d87 启动签名** | 日 `\|d87\|_max < th` → 全天强制 OFF | `day_absmax_th=110` |
| **B. 启动日双阈值** | 启动日用 `startup_thr` 重判 `p_on`，与原 state 取并集 | `startup_thr=0.40` |
| **C. 功率标定** | `scale = train_on_mean / pred_on_mean` 有界缩放 | `train_on_mean_w=736` |

**为何不用 bus_lift**：离线证明 FP 日与 TP 日的 d73 lift 分布重叠（均值均 ~1700W+），物理上不可分。  
**为何 d87 日级有效**：训练 OFF 日 `d87_absmax` 中位 ~91，ON 日中位 ~187；`th=100~110` 可拦 7/02–04 且保留 7/05+。

代码：
- 新模块 `scripts/bus_consistency_guard.py`
- 接入 `scripts/05_inference.py`（d87 守卫之后 / L4 之前）
- 配置透传 `run_batch_users.py` ← `time_filters.json` → env `NILM_BUS_GUARD_JSON`

---

## 3. 验证结果（修复后）

### 3.1 整体 OOD

| 指标 | 修复前 | **修复后** | Δ |
|------|--------|------------|---|
| **ood_F1** | 0.897 | **0.975** | **+0.078** |
| ood_P | 0.892 | **0.973** | +0.081 |
| ood_R | 0.901 | **0.977** | +0.076 |
| **ood_MAE** | 72.9 W | **38.5 W** | **−47%** |
| ood_SAE | 11.9% | **11.9%** | ≈0 |
| kWh true/pred | 84.2 / 94.2 | 84.2 / 94.2 | 电量偏差持平 |

### 3.2 事故日对比

| 日期 | 修复前 FP/FN | **修复后 FP/FN** | 修复前 F1 | **修复后 F1** |
|------|--------------|------------------|-----------|---------------|
| **7/01** | (有假开风险) | **0/0** 全 OFF | — | 全 OFF 正确 |
| **7/02** | 15/0 | **0/0** | 0.00 | 全 OFF 正确 |
| **7/03** | 4/0 | **0/0** | 0.00 | 全 OFF 正确 |
| **7/04** | 25/0 | **0/0** | 0.00 | 全 OFF 正确 |
| **7/05** | 0/36 | **0/7** | 0.10 | **0.899** |

守卫日志：
```
force_off_days=['2026-07-01','2026-07-02','2026-07-03','2026-07-04']
fn_recall=41, ON 491->488
```

### 3.3 其余 ON 日（抽样）

| 日期 | F1 | FP/FN | MAE(W) |
|------|-----|-------|--------|
| 7/08 | 1.000 | 0/0 | 60 |
| 7/11 | 1.000 | 0/0 | 34 |
| 7/14 | 1.000 | 0/0 | 10 |
| 7/15 | 0.936 | 6/0 | 55 |

---

## 4. 残余问题与后续

1. **7/05 仍 FN=7**（R=0.816）：启动日 thr=0.40 已救回大部分，边界段仍漏；可考虑 thr→0.35 或短 run 填充。  
2. **7/06 FP=5 / F1=0.92**：启动日低阈值带来少量假开；可加最小连续 ON 或晚间先验。  
3. **功率标定 scale 未触发**（pred 均值已接近 train_on_mean）：SAE 仍 ~12%，ON 段绝对误差可再做分位回归/残差 L4 加强。  
4. **推广**：其它用户默认 `bus_guard.enabled=false`；仅对“关 d87 守卫 + 存在 OFF 日假开”的用户打开。

---

## 5. 配置片段

```json
"800080252844_4206894986488": {
  "guard_enabled": false,
  "target_col": "p2",
  "bus_guard": {
    "enabled": true,
    "version": "v14.1.1_d87_day_power",
    "day_absmax_th": 110.0,
    "startup_thr": 0.40,
    "train_on_mean_w": 736.0,
    "power_calib_enable": true
  }
}
```

---

## 6. 结论

P0 目标 **达成**：
- OFF 日假开（7/02–04）**清零**  
- 7/05 漏检 F1 **0.10 → 0.90**  
- 整体 ood_F1 **0.897 → 0.975**，MAE **72.9 → 38.5 W**  

方案可配置、非侵入，不影响未开启 `bus_guard` 的其它用户。
