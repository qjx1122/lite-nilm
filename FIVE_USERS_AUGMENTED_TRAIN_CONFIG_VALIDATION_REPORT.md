# 5用户“补充训练数据”配置验证报告

> 用户要求：修改用户配置文件，从训练集之外的总线与分路数据中补充需要的数据重新训练，并使用剩余数据验证。  
> 实验配置：`data/time_filters_augmented_forceoff.json`  
> 基线配置：`data/time_filters.json`  
> 批跑命令：`.venv/bin/python scripts/run_batch_users.py --force-retrain --time-filter-config data/time_filters_augmented_forceoff.json`  
> 批跑日志：`logs/_batch/batch_run_20260804_073928.log`  
> 结果：5/5 用户训练推理成功，但整体指标显著退化，**不建议应用生产**。

---

## 1. 配置变更说明

本次未覆盖原始 `data/time_filters.json`，而是新增实验配置：

```text
data/time_filters_augmented_forceoff.json
```

原则：

- 只补充 6 月数据进入训练；
- 7 月仍作为 OOD 验证；
- 新增训练日同步从 inference 验证中排除，避免 train/infer 日期重叠。

新增训练日期：

| 用户 | 新增训练日期 | 目的 |
|---|---|---|
| U842 | 6/8, 6/9, 6/19, 6/21, 6/27 | 梅雨/低功率/长时ON样本 |
| U2844 | 6/19, 6/20, 6/22, 6/28 | FN/低真值高FP样本 |
| U0778 | 6/20, 6/24, 6/25 | OFF虚块/低真值高FP样本 |
| U0789 | 6/5, 6/6, 6/9, 6/10, 6/20 | OFF/非目标样本 |
| U0800 | 6/2-6/6, 6/13-6/15 | OFF样本 + 少量ON样本 |

---

## 2. 整体结果：原配置 vs 补充训练配置

| 口径 | pooled F1 | Precision | Recall | SAE | kWh_true | kWh_pred | TP | FP | FN | TN |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 原配置 baseline | 0.8495 | 0.7743 | 0.9408 | 10.08% | 1113.10 | 1225.33 | 6004 | 1750 | 378 | 8568 |
| 补充训练配置 | 0.5836 | 0.8616 | 0.4413 | 64.20% | 1068.07 | 382.37 | 2584 | 415 | 3272 | 8031 |

结论：

```text
Precision 提升，但 Recall 崩溃。
FN 从 378 增加到 3272。
总预测电量从 1225.33 kWh 降到 382.37 kWh。
整体 SAE 从 10.08% 恶化到 64.20%。
```

---

## 3. 分用户结果对比

| 用户 | F1 原配置 | F1 补充训练 | Recall 原配置 | Recall 补充训练 | SAE 原配置 | SAE 补充训练 | 主要变化 |
|---|---:|---:|---:|---:|---:|---:|---|
| U842 | 0.9144 | 0.9416 | 0.8560 | 0.9375 | 14.60% | 26.20% | F1改善但总量低估扩大，SAE恶化 |
| U2844 | 0.8953 | 0.9293 | 0.8640 | 0.9921 | 11.21% | 11.03% | F1/Recall改善，Precision下降，整体可接受 |
| U0778 | 0.9458 | 0.4907 | 0.9912 | 0.3257 | 0.38% | 66.05% | 严重漏检，预测电量崩溃 |
| U0789 | 0.7992 | 0.0000 | 0.9953 | 0.0000 | 26.55% | 100.00% | 模型几乎全关断，完全失败 |
| U0800 | 0.7227 | 0.4503 | 0.9568 | 0.3665 | 19.57% | 55.50% | Recall大幅下降，严重低估 |

---

## 4. 关键失败原因

### 4.1 直接把 OFF/异常日加入主训练，会改变主分类器边界

本次本意是补充 force-off / 电路一致性所需样本。但这些样本直接进入主分类器训练后，主分类器学到更保守的边界，导致大量 ON 点被压成 OFF。

尤其：

| 用户 | best_thr 原配置 | best_thr 补充训练 | 结果 |
|---|---:|---:|---|
| U0789 | 0.02 | 0.39 | 推理 TP=0，Recall=0 |
| U0778 | 0.05 | 0.14 | Recall 从 0.991 降到 0.326 |
| U0800 | 0.37 | 0.35 | 虽阈值相近，但训练分布改变后概率整体下移，Recall=0.366 |

### 4.2 新增 OFF 样本应训练“后处理守卫”，不应直接重塑主 NILM 分类器

force-off 的目标是：

```text
当主模型高置信误报时，用电路一致性信号否决。
```

它应作为：

```text
post-classifier guard / consistency filter
```

而不是简单把 OFF 日加入主训练集，让主模型整体变保守。

### 4.3 U0789/U0800 原问题是“模型输出高置信 ON”，不是训练集中缺少少量 OFF 日即可解决

U0789/U0800 的 OFF 虚块往往表现为：

```text
p_on 很高
pred_on_n 很多
pred_kWh 很大
```

仅把若干 OFF 日加入主模型训练，会使主模型在验证集上选择更高阈值或更保守概率，但不能形成可靠的物理一致性判断，反而会误杀真实 ON。

### 4.4 U0778 的总量 SAE 抵消假象被打破后，主模型缺少功率补偿

U0778 原配置总量 SAE 很好，是因为：

```text
OFF 日 FP 高估 与 ON 日低估 互相抵消。
```

补充训练后主模型减少预测 ON，但 ON 日低估问题未解决，导致总量严重低估。

---

## 5. 是否可以应用生产？

结论：

```text
[FAIL] 不建议将 data/time_filters_augmented_forceoff.json 应用于生产。
```

原因：

1. 5用户 pooled F1 从 0.8495 降到 0.5836；
2. Recall 从 0.9408 降到 0.4413；
3. FN 从 378 暴增到 3272；
4. SAE 从 10.08% 恶化到 64.20%；
5. U0789 完全失败，U0778/U0800 严重退化。

---

## 6. 正确的生产化方向

### 6.1 保留原始主训练配置

生产主路径仍应使用：

```text
data/time_filters.json
```

不要用本次实验配置替代。

### 6.2 将新增 6 月样本用于训练“守卫”，而不是主分类器

建议新增独立 guard 数据集：

```text
force_off_guard_train_days
force_off_guard_val_days
```

守卫输入不应只用主模型输出，还应加入物理信号：

```text
raw bus 段内形态
启动/停止响应
非空调基线估计
目标支路功率档位一致性
天气/时段上下文
coverage/no-positive/off-FP flags
```

### 6.3 两阶段结构

推荐结构：

```text
主 NILM 分类器保持原训练配置
        ↓
主模型给出 state_pred / p_on / y_pred
        ↓
force-off consistency guard 用补充样本训练
        ↓
仅在 guard 高置信判定“非目标/电路错位”时 force-off
```

### 6.4 每个用户的下一步

| 用户 | 下一步 |
|---|---|
| U0789 | 用新增 OFF 日训练电路一致性守卫，不改主分类器；重点防止高置信 OFF 虚块 |
| U0800 | 同 U0789，同时补高温 ON 样本做功率补偿 |
| U0778 | 训练 OFF 虚块守卫后，单独补偿 ON 日高温低估 |
| U842 | 继续 P0-P3 focused guards 的 train/val 泛化选型，不采用本次增强主训练结果 |
| U2844 | 可参考新增样本做 recall/low-true guard，但需保持 bus_guard 滑窗锚 |

---

## 7. 最终判断

本次实验验证了一个重要结论：

> **补充 6 月 OFF/异常日样本是必要的，但不能直接合入主 NILM 分类器训练。它们应作为独立 force-off / consistency guard 的训练样本。**

生产建议：

```text
[OK] 保留 P4 日报字段生产化。
[OK] 保留原始 data/time_filters.json 主训练配置。
[FAIL] 不上线 data/time_filters_augmented_forceoff.json。
[NEXT] 用补充样本训练独立电路一致性守卫，并在原主模型之后后处理。 
```
