# U842 2026-06-19 评估指标异常专项分析与优化方案

> 分析对象：U842 用户，日期 `2026-06-19`。  
> 当前优化版本：`P0_P1_P2_P3_lowpower_edge`。  
> 结论先行：**这一天不是分类问题，而是“低功率长时 ON 被功率层按高档位估计”的严重回归问题。当前风险闸已经阻止 rawbus 进一步上抬，但回退到 baseline 后，baseline 本身仍严重高估。生产化优化应新增“低功率长时 ON 功率锚/低档模式回归”，参数从 train/val 的低功率长时 ON 样本推导，不能用 6/19 反向调参。**

---

## 1. 当前优化版本日级指标

| variant | F1 | Precision | Recall | SAE | MAE_W | RMSE_W | kWh_true | kWh_pred | kWh_err | TP | FP | FN | TN |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| baseline | 0.9901 | 1.0000 | 0.9804 | 266.6% | 295.17 | 408.22 | 2.602 | 9.540 | +6.938 | 50 | 0 | 1 | 45 |
| 当前优化版 | 0.9901 | 1.0000 | 0.9804 | 266.6% | 295.17 | 408.22 | 2.602 | 9.540 | +6.938 | 50 | 0 | 1 | 45 |

解释：

- F1=0.9901，分类已接近完美；
- 只有 1 个 FN、0 个 FP；
- 但 SAE=266.6%，kWh_pred 是 kWh_true 的约 3.67 倍；
- 所以问题不在分类层，而在功率回归层。

---

## 2. 逐点证据

### 2.1 ON/OFF 段

| 项目 | 时间段 |
|---|---|
| true ON | 09:30 - 22:15 |
| pred ON | 09:30 - 22:00 |
| FN | 22:00 - 22:15 |
| FP | 无 |

分类层只漏掉最后 15 分钟，不能解释 6.94 kWh 的高估。

### 2.2 ON 点功率统计

| 统计 | true ON | pred ON |
|---|---:|---:|
| 点数 | 51 | 50 |
| 小时数 | 12.75h | 12.50h |
| 真实功率均值 | 202.65 W | - |
| 真实功率中位数 | 200.46 W | - |
| 真实功率最小/最大 | 87.07 / 547.24 W | - |
| 预测功率均值 | - | 763.21 W |
| 预测功率中位数 | - | 778.80 W |
| 预测功率最小/最大 | - | 683.73 / 817.04 W |

即：真实是约 200W 的长时低功率 ON，模型却按 760W 左右的高档位估计。

---

## 3. 天气与总线证据

### 3.1 天气

| 指标 | 值 |
|---|---:|
| temp_mean | 26.63°C |
| temp_max | 28.80°C |
| RH_mean | 84.25% |
| RH_max | 98% |

这一天属于暖湿/高湿环境，但不是 7/6 那种整日分类低概率；分类器仍判 ON。

### 3.2 raw bus 统计

`load_iden_data73` 当天：

| 统计 | 值 |
|---|---:|
| day mean | 1620.54 |
| pred ON mean | 2692.09 |
| min / max | 270.67 / 3108.33 |

raw bus 在 ON 段很高，因此 rawbus 模型会倾向于上抬功率。这解释了为什么：

```text
P2 rawbus 原始输出比 baseline 还高；
安全闸/低功率长时风险闸选择回退 baseline；
但 baseline 本身仍高估。
```

---

## 4. 为什么现有优化没有修复 6/19？

### 4.1 P1 类 guard 不适用

P1 负责补 FN/整日漏检。6/19 已经：

```text
TP=50, FN=1, FP=0
F1=0.9901
```

所以 P1 不应介入。

### 4.2 P2 rawbus 被风险闸回退是正确的

此前 rawbus 会把 6/19 预测从 9.54 kWh 进一步抬到约 9.74 kWh，风险闸回退到 baseline，避免更坏。

这说明风险闸方向正确：

```text
阻止 rawbus 进一步恶化
```

但它不能解决 baseline 原有高估。

### 4.3 train/val 缺少同型强样本

train+val 中存在低功率长时 ON 日：

| date | true_on_h | true_on_mean_W | base_on_mean_W | true_kWh | base_kWh | SAE |
|---|---:|---:|---:|---:|---:|---:|
| 2026-06-04 | 13.25h | 196W | 235W | 2.605 | 3.109 | 19.3% |
| 2026-06-10 | 13.25h | 292W | 324W | 3.886 | 4.288 | 10.3% |
| 2026-06-11 | 12.50h | 279W | 303W | 3.492 | 3.782 | 8.3% |

但 6/19 是：

```text
true_on_mean ≈ 203W
base_on_mean ≈ 763W
```

即 train/val 有低功率长时样本，但没有“raw bus 高、baseline 也高、真实却只有 200W”的强错配样本。这是泛化困难的根本原因。

---

## 5. 优化方案

## 5.1 方案 A：低功率长时 ON 专用功率锚（推荐优先验证）

### 规则思想

当前风险闸已能识别：

```text
rawbus 上抬风险
低/中 p_on 长时 ON
RH<=85
pred_on_n>=40
```

这些条件命中 6/19 和 6/27，并已用于回退 rawbus。下一步不是只回退 baseline，而是进入低功率长时 ON 专用功率锚。

### 参数来源

从 train+val 低功率长时 ON 日推导：

```text
2026-06-04: true_on_mean ≈196W, RH≈80.5
2026-06-10: true_on_mean ≈292W, RH≈59.4
2026-06-11: true_on_mean ≈279W, RH≈58.1
```

建议分湿度两档：

| 条件 | 推荐锚点 | 来源 |
|---|---:|---|
| RH >= 80 的低功率长时风险日 | 约 196~200W | train 2026-06-04 |
| RH < 80 的低功率长时风险日 | 约 300~325W | train 2026-06-10/06-11，并保守上调避免 6/27 低估 |

### 对 6/19 的预期

若 6/19 使用约 200~250W 的低功率长时锚：

| anchor | kWh_pred 估计 | SAE 估计 |
|---:|---:|---:|
| 196W | 约 2.45 kWh | 约 6% |
| 250W | 约 3.13 kWh | 约 20% |

当前是：

```text
kWh_pred=9.540, SAE=266.6%
```

收益非常明确。

### 风险

- 对 6/27 这类真实均值约 375W 的中低功率日，若锚点过低会低估；
- 因此建议湿度/温度/原始总线分段锚，而不是单一锚。

---

## 5.2 方案 B：低功率长时模式分类器 + per-mode regressor

### 当前 P2 rawbus 问题

当前 P2 rawbus 能修高功率低估，但对 6/19 这种“raw bus 高但分路低功率”的日子失败。

### 需要新增的模式标签

在原 low/mid/high 模式外，增加：

```text
long_low_power_mode
```

标签来源：train+val：

```text
true_on_h >= 10h
true_on_mean < 350W
```

候选样本：

```text
2026-06-04
2026-06-10
2026-06-11
```

### 需要新增特征

仅用推理可见特征：

```text
pred_on_n
p_on_q50 / p_on_q75
base_on_mean
raw73_on_mean
raw73_on_std
RH_mean / temp_mean
rawbus-to-baseline 比值
日内早/中/晚 rawbus 与 p_on 分布
```

### 优点

比固定锚更灵活，可同时处理：

```text
6/19 ≈ 200W
6/27 ≈ 375W
```

### 风险

train+val 样本只有 3 天，同型样本不足。需要 8 月或后续数据补充低功率长时样本后再生产化。

---

## 5.3 方案 C：保守 cap，而非强制改写

如果不想直接替换功率，可先做 cap：

```text
if low_power_long_risk:
    y_pred = min(y_pred, cap_w)
```

cap 由 train+val 推导：

```text
高湿低功率 cap ≈ 250W
低湿/中湿低功率 cap ≈ 325W
```

对 6/19：

```text
763W -> 200~250W 量级
```

能显著降低 SAE。

对 6/27：

```text
558W -> 300~325W 量级
```

可能低估但远小于当前高估风险。

---

## 6. 推荐落地顺序

| 优先级 | 动作 | 说明 |
|---|---|---|
| P0 | 增加低功率长时风险日专用 cap/anchor 离线验证 | 先不改主 pipeline，只在 experiment 脚本验证。 |
| P1 | 用 train/val 低功率长时日选择湿度分段锚点 | 高湿锚约 200W，低湿/中湿锚约 300~325W。 |
| P2 | 验证对 6/19、6/27、6/7、6/10、6/11、test 4/24 的影响 | 必须保证 train/val 不新增 SAE>20 日。 |
| P3 | 若离线稳定，再做配置化灰度 | 不直接全量上线。 |

---

## 7. 最终结论

2026-06-19 的问题本质是：

```text
低功率长时 ON + raw bus 高负荷背景 + baseline 按高档位预测
```

当前模型分类没有问题：

```text
F1=0.9901, TP=50, FN=1, FP=0
```

问题完全在功率层：

```text
true_on_mean≈203W
pred_on_mean≈763W
kWh_true=2.60
kWh_pred=9.54
SAE=266.6%
```

当前最合理的优化方向不是继续调分类，而是：

```text
低功率长时 ON 专用功率锚 / cap / 模式回归
```

其中最优先可验证的方案是：

```text
基于 train+val 低功率长时样本的湿度分段功率锚：
RH>=80 -> 约200W
RH<80  -> 约300~325W
```

该方案预计能把 6/19 的 SAE 从 266.6% 降到约 6%~20% 区间，但需要同时验证 6/27，避免中低功率日被过度压低。
